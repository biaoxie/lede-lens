# Brand sources

`brand/` contains the editable vector sources for LedeLens's Open Frame mark.
The generated Chrome runtime icons live in `assets/icons/`.

When the mark changes, render and inspect all four manifest sizes. The 16px
source is intentionally simplified and should not be replaced by automatic
downsampling of the larger mark.
